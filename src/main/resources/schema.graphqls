type Query {
    userById(id: ID): User
}

type User {
    id: ID
    name: String
    age: Int
    address: Address
}

type Address {
    id: ID
    houseName: String
    country: String
}

type Mutation {
createUser(name: String!, age: Int!): User
}